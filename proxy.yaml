include:
  - app.yaml
services:
  proxy:
    image: nginx:alpine
    container_name: 68-s1-proxy
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - s1app
